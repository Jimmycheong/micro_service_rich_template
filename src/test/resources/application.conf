psqldb = {
  connectionPool = "HikariCP" //use HikariCP for our connection pool
  jdbcUrl = "jdbc:h2:mem:testdbs;DATABASE_TO_LOWER=TRUE;MODE=PostgreSQL;DB_CLOSE_DELAY=-1"
  properties = {
    driver = "org.postgresql.Driver"
    serverName = "localhost"
    portNumber = "5432"
    user = "tom"
    password = "tom"
  }
  migrationsFolder = "db/"
  numThreads = 10
}
